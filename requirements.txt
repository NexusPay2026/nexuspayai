# ═══════════════════════════════════════════════════════════════
#  NexusPay Intelligence — Unified Backend Dependencies
# ═══════════════════════════════════════════════════════════════

# ── Core Framework ───────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
python-multipart==0.0.18  # bumped from 0.0.9 — CVE-2024-53981 (multipart DoS)

# ── Database (Postgres only) ─────────────────────────────────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
databases[asyncpg]==0.9.0
psycopg2-binary==2.9.10
alembic==1.13.3

# ── AI Provider HTTP Client ──────────────────────────────────
httpx==0.27.2

# ── Cloudflare R2 (S3-compatible) ────────────────────────────
boto3==1.35.0

# ── Password hashing (CMS console admin sign-in) ─────────────
# The portal's own auth uses stdlib PBKDF2 (app/services/auth_service.py). The
# CMS console verifies against a bcrypt hash supplied via CMS_ADMIN_PASSWORD_HASH;
# generate it locally with scripts/make_admin_hash.py.
bcrypt==4.2.1

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1

reportlab>=4.0

pypdf==4.3.1

# ── Statement page rendering (multi-page extraction: rasterize scanned PDFs, downscale images) ──
PyMuPDF==1.24.10
Pillow==10.4.0
