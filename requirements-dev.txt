# Test + CI-only dependencies (NOT installed on the production Render service).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.2,<10
pytest-asyncio>=0.24,<2
aiosqlite>=0.20          # in-memory async test database (portable model path)
pip-audit>=2.7           # dependency vulnerability scan (CI)
